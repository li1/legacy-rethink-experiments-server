{
  "name": "rethink-experiments-server",
  "version": "1.0.0",
  "main": "src/app.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "npx babel src --out-dir lib",
    "start": "yarn run build && standard --fix && node lib/app.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "cors": "^2.8.4",
    "express": "^4.16.4",
    "http": "^0.0.0",
    "rethinkdb": "^2.3.3",
    "socket.io": "^2.1.1"
  }
}
